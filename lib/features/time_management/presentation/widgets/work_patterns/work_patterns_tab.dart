import 'package:flutter/material.dart';

class WorkPatternsTab extends StatelessWidget {
  const WorkPatternsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Work Patterns Tab - Coming Soon'));
  }
}
